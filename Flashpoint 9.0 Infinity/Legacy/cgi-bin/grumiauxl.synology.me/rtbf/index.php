<?php

	$service = $_REQUEST['service'];
	
	if ($service == '2') {	// Get user id service
		echo '44';
	} else if ($service == '8') {	// Get missions service
		// Return real missions, in .xml file there's a fake mission as an example
	}