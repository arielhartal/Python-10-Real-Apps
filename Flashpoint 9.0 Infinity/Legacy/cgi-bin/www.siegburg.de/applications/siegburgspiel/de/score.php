//Wenn nicht erfolgreich, bitte nahfolgenden Teil zwischen die PHP (unterer Teil) kopieren!!!!
// if ($action == "schreiben") {
	// include("config.php");

	// Datenbankverbindung herstellen
	// $connection = mysql_connect($host, $user, $pass) or die ("Kann keine Verbindung herstellen!");

	// Datenbank ausw‰hlen
	// mysql_select_db($db, $connection) or die ("Kann keine Datenbank ausw‰hlen!");

	// Datenbankeintrag 
	// Datenbankanfrage generieren
	// $query = "INSERT INTO highscore (name, strasse, ort, email, telefon, datum, punkte)
				// VALUES('$name','$strasse','$ort','$email', '$telefon', NOW(), '$points')";
	// $result = mysql_query($query, $connection) or die ("Fehler in Abfrage: $query. " . mysql_error());
	
	// echo "Message=eingetragen.";
	
	// Datenbankverbindung schlieﬂen
	// mysql_close($connection);
// }



